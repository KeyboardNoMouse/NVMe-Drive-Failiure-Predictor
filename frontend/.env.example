# URL of the deployed FastAPI backend (no trailing slash).
# Locally this defaults to http://localhost:8000 if unset.
# In Vercel, set this as a Project > Settings > Environment Variable.
NEXT_PUBLIC_API_URL=https://your-backend-host.example.com
